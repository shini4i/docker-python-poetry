name: Build and Publish docker images (python 3.10)

on:
  push:
    branches:
      - main
    paths:
      - "3.10/**"

jobs:
  build-and-push-docker-image:
    name: Build Docker image and push to repositories
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Github Packages
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Extract python version
        run: echo "PYTHON_VERSION=$(grep FROM 3.10/Dockerfile | awk -F'[:-]' '{ print $2 }')" >> $GITHUB_ENV

      - name: Extract poetry version
        run: echo "POETRY_VERSION=$(grep POETRY_VERSION= 3.10/Dockerfile | awk -F'=' '{ print $2 }') >> $GITHUB_ENV

      - name: Build image and push to GitHub Container Registry
        uses: docker/build-push-action@v3
        with:
          context: 3.10
          tags: |
            ghcr.io/shini4i/python-poetry:3.10-${{ $POETRY_VERSION }}
            ghcr.io/shini4i/python-poetry:${{ $PYTHON_VERSION }}-${{ $POETRY_VERSION }}
          push: true
